// resume.js - Logic specific to the Resume section

document.addEventListener('DOMContentLoaded', () => {
    const resumeSection = document.getElementById('resume');
    if (!resumeSection) {
        // console.log("Resume section not found on this page, resume.js will not run.");
        return;
    }

    console.log("Resume section specific JS initializing...");

    // Add any resume-section specific JS interactions here.
    // For example:
    // - If clicking on a resume item expanded more details inline.
    // - If you had tabs for different types of resume entries (e.g., work, education, volunteer).
});